# 02 — Backbone Fine-Tuning on BRACS

This folder contains the notebooks for fine-tuning feature extraction backbones on the **BRACS** dataset (7-class patch-level classification), producing domain-adapted encoders for downstream WSI-level staging.
